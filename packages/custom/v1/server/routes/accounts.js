(function () {
  'use strict';

  /* jshint -W098 */
  // The Package is past automatically as first parameter
  module.exports = function (V1, app, auth, database) {
//     var accounts = require('../controllers/accounts')(V1);
//
//     app.route('/admin/v1/accounts')
//       .get(auth.requiresLogin, auth.requiresUserAdmin, accounts.all)
//       .post(auth.requiresLogin, auth.requiresUserAdmin, accounts.create);
//
//     app.route('/admin/v1/accounts/checker')
//       .get(auth.requiresLogin, auth.requiresUserAdmin, accounts.checker);
//
//     app.route('/admin/v1/accounts/sessions')
//       .get(auth.requiresLogin, accounts.getSessions);
//
//     app.route('/admin/v1/accounts/sessions/:clientId')
//       .delete(auth.requiresLogin, accounts.revokeToken);
//
//     app.route('/admin/v1/accounts/:account_accountId')
//       .get(auth.isMongoId, auth.requiresLogin, auth.requiresUserAdmin, accounts.show)
//       .put(auth.isMongoId, auth.requiresLogin, auth.requiresUserAdmin, accounts.update)
//       .delete(auth.isMongoId, auth.requiresLogin, auth.requiresUserAdmin, accounts.delete);
//
//     app.route('/admin/v1/accounts/:account_accountId/roles')
//       .put(auth.requiresLogin, accounts.updateAccountRoles);
//
//     // Setting up params
//     app.param('account_accountId', accounts.account);
  };
})();

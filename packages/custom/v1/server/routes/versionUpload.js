'use strict';

/* jshint -W098 */
// The Package is past automatically as first parameter
module.exports = function(Upload, app, auth, database) {
//   // var multipart = require('connect-multiparty');
//   // var multipartMiddleware = multipart();
//   var versionUpload = require('../controllers/versionUpload')(Upload);
//
//   app.route('/utility/v1/upload/version')
//   .post(auth.requiresLogin, versionUpload.processUpload);
//
//   // app.param('upload_document_name', upload.filename);
};
